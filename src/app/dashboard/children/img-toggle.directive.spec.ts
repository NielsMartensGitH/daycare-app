import { ImgToggleDirective } from './img-toggle.directive';

describe('ImgToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgToggleDirective();
    expect(directive).toBeTruthy();
  });
});
