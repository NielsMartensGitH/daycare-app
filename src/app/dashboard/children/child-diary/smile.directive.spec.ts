import { SmileDirective } from './smile.directive';

describe('SmileDirective', () => {
  it('should create an instance', () => {
    const directive = new SmileDirective();
    expect(directive).toBeTruthy();
  });
});
