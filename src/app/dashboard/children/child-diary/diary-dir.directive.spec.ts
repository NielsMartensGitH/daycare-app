import { DiaryDirDirective } from './diary-dir.directive';

describe('DiaryDirDirective', () => {
  it('should create an instance', () => {
    const directive = new DiaryDirDirective();
    expect(directive).toBeTruthy();
  });
});
